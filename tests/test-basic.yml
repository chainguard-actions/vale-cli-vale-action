name: Test basic usage - Vale on specific folder
on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up git origin reachable inside Docker container
        run: |
          git config user.email "test@example.com"
          git config user.name "Test User"
          rm -rf "$GITHUB_WORKSPACE/.git-remote"
          git init --bare "$GITHUB_WORKSPACE/.git-remote"
          git push "$GITHUB_WORKSPACE/.git-remote" HEAD:refs/heads/act-harness
          git remote set-url origin "$GITHUB_WORKSPACE/.git-remote" 2>/dev/null || \
            git remote add origin "$GITHUB_WORKSPACE/.git-remote"

      - name: Set up Vale config in workspace
        run: |
          # Copy the fixture vale.ini to the workspace root so Vale can find it
          cp tests/fixtures/vale-config/.vale.ini "$GITHUB_WORKSPACE/.vale.ini"

      - name: Run Vale action on specific folder
        id: vale
        uses: .
        with:
          version: "latest"
          files: "tests/fixtures/docs"
          debug: "true"
          reporter: "github-check"
          fail_on_error: "false"
          level: "error"
          filter_mode: "nofilter"
        continue-on-error: true

      - name: Assert action completed
        run: |
          echo "Vale action step completed (exit code captured via continue-on-error)"
          echo "PASS: Vale action ran without crashing the workflow"

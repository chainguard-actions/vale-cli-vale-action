name: Test specific Vale version
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create Vale config
        run: |
          mkdir -p "$GITHUB_WORKSPACE/version-test-docs"
          cat > "$GITHUB_WORKSPACE/version-test-docs/.vale.ini" <<'EOF'
          StylesPath = styles
          MinAlertLevel = suggestion

          [*.md]
          BasedOnStyles = Vale
          EOF
          cat > "$GITHUB_WORKSPACE/version-test-docs/sample.md" <<'EOF'
          # Version Test

          This is a test document for version testing.
          EOF

      - id: action
        uses: .
        continue-on-error: true
        with:
          version: "3.9.5"
          files: version-test-docs
          config: "${{ github.workspace }}/version-test-docs/.vale.ini"
          sync: "false"
          reporter: "github-pr-annotations"
          fail_on_error: "false"
          filter_mode: "nofilter"

      - name: Assert action ran with specific version
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "success" ]; then
            echo "PASS: action completed successfully with specific Vale version"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (expected with sentinel repo)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
